import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, isSupabaseConfigured } from './supabase-config.js';
const root=document.getElementById('profile-activity'), count=document.getElementById('profile-activity-count'), profileId=new URLSearchParams(location.search).get('id');
function url(value){try{const u=new URL(String(value||'').trim());return['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}}
function date(value){const d=new Date(value);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(d)}
function eventDate(value){if(!value)return'';const d=new Date(`${value}T12:00:00`);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(d)}
function render(rows){root.replaceChildren();count.textContent=`${rows.length} ${rows.length===1?'post':'posts'}`;if(!rows.length){const p=document.createElement('p');p.className='review-empty';p.textContent='No community posts yet.';root.append(p);return}for(const post of rows){const article=document.createElement('article');article.className='profile-activity-card';const badge=document.createElement('span');badge.className='activity-post-badge';badge.textContent=post.post_type==='event'?'GOING TO':post.post_type==='listening'?'NOW LISTENING':'POST';article.append(badge);if(post.title){const title=document.createElement('strong');title.textContent=post.title;article.append(title)}let detail='';if(post.post_type==='event')detail=[eventDate(post.event_date),post.city].filter(Boolean).join(' · ');if(post.post_type==='listening'&&post.subtitle)detail=`by ${post.subtitle}`;if(detail){const p=document.createElement('p');p.textContent=detail;article.append(p)}if(post.caption){const p=document.createElement('p');p.textContent=post.caption;article.append(p)}const image=url(post.image_url);if(image){const img=document.createElement('img');img.className='profile-activity-image';img.src=image;img.alt=post.title?`Image for ${post.title}`:'Community post image';img.loading='lazy';article.append(img)}const link=url(post.media_url);if(link){const a=document.createElement('a');a.className='retro-button';a.href=link;a.target='_blank';a.rel='noopener';a.textContent=post.post_type==='event'?'Event page':post.post_type==='listening'?'Listen':'Open link';article.append(a)}const small=document.createElement('small');small.textContent=date(post.created_at);article.append(small);root.append(article)}}
if(!root||!profileId||!isSupabaseConfigured()){if(root)root.innerHTML='<p class="review-empty">Community activity is unavailable.</p>'}else{const{createClient}=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.112.1/+esm');const supabase=createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY);const result=await supabase.from('activity_posts').select('id,post_type,title,subtitle,caption,media_url,image_url,event_date,city,created_at').eq('user_id',profileId).eq('visible',true).order('created_at',{ascending:false}).limit(20);if(result.error)root.innerHTML='<p class="review-empty">Activity could not load.</p>';else render(result.data||[])}
