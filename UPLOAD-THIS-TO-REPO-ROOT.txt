OPTICBOX NETWORK v32 — ROOT READY
================================
Upload the CONTENTS of this ZIP to the root of the opticbox-network GitHub repository.
Replace matching files.

v32 FIREFOX AUDIO CHANGE
- Firefox versions of 0PTICSCOPE, 0PS3, and SPECTRAVAULT default to 0PTIC'S AUDIO BRIDGE.
- Bridge download: downloads/0ptics-audio-bridge-windows.zip
- Bridge listens only on 127.0.0.1:17491 and reads the Windows default output meter.
- Audio Input / Audio File / Screen Audio remain available as fallback sources.
- No new Supabase SQL is required.

Keep your existing supabase-config.js. It is intentionally not included here.
