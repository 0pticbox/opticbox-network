0PTICBOX NETWORK v28 — FOLLOWER TOTAL NULL-FALLBACK FIX

Upload the CONTENTS of this folder to the root of your GitHub repository, replacing matching files.
Keep your existing supabase-config.js if GitHub already has your working Supabase URL and publishable key.

WHAT CHANGED
1. Fixed the Total Followers dash on the existing 0PTICBOX profile.
2. v27 only applied string fallbacks; numeric social-count fallbacks were ignored when the new Supabase columns were NULL.
3. The existing public Instagram snapshot already present in the project (957 followers, Aug 5 2026) is now used as the 0PTICBOX fallback until a newer count is saved in Profile Settings.
4. Any Instagram/YouTube numbers saved by the member still win over the fallback.
5. Total Followers remains Instagram followers + YouTube subscribers.
6. Other members are never given invented counts; they set their own public counts in Profile Settings.

SUPABASE
Run supabase/2026-08-profile-follower-counts-v28.sql once. It safely adds the count columns and backfills only the known 0PTICBOX Instagram snapshot when that value is still NULL.
