OPTICBOX NETWORK v30 — FIREFOX COMPATIBILITY

Patched browser apps:
- 0PS3: Firefox-safe screen sharing, WebM recording, floating-window fallback for PiP.
- SPECTRAVAULT: MP4 when supported, automatic WebM fallback (including Firefox), floating-window fallback for PiP, Firefox desktop-audio guidance.
- 0PTICSCOPE: Firefox-safe desktop-audio handling and WebM recorder setup.
- DISTORTION: explicit canvas-recording feature detection; supported encoder list remains automatic.
- DJ Visual Studio: already had popup detachment + WebM encoder fallback; help text now documents Firefox behavior.
- BOXED ARP and PICO-8 games use browser-standard APIs and require no Firefox-specific fork.

Important Firefox limitation:
Screen/window sharing may be video-only depending on the capture source and Firefox/OS support. The apps now fail gracefully and direct the user to Microphone or Audio File instead of appearing broken.
